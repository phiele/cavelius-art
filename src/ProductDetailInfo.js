// import Button from "./Button.js";

export default function ProductDetailInfo({ product, onProductPlus }) {
  return (
    <>
      <p className="product-details-info">
        Trendy art to add to your wall if you are looking for subtle and fashionable colours.
      </p>
      {/* <span>{product.description}</span> */}
      {/* <h2>€{product.price}</h2> */}
      {/* <Button onClick={() => onProductPlus(product)}>Add to cart</Button> */}
    </>
  );
}
